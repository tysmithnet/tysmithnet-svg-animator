"use strict";

import angular from "angular";

/**
 * Viewer component module
 * The viewer component is the component responsible for
 * rendering the scene and displaying it for preview.
 */
angular
    .module("viewer", []);

